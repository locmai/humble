apiVersion: v1
kind: Service
metadata:
  name: {{ include "djangodemo.fullname" . }}
  labels:
    {{- include "djangodemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djangodemo.selectorLabels" . | nindent 4 }}
