apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djangodemo.fullname" . }}-test-connection"
  labels:
    {{- include "djangodemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djangodemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
